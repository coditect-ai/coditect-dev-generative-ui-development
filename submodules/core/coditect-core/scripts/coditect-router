#!/usr/bin/env bash
# CODITECT Command Router - Simple Wrapper
# Usage: coditect-router "your request here"
#        coditect-router -i  (interactive mode)

SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
exec python3 "$SCRIPT_DIR/coditect-command-router.py" "$@"
